# 提交前说明

当前已生成课程要求的提交目录：

`submission/241098117-张城宗/`

已完成：

- 根目录 `README.md`
- 根目录 `AI_CODING_SUMMARY.md`
- 清理后的 `src/`
- 提交目录运行验证

仍需人工完成：

1. 将录屏命名为 `demo.mp4`，放入 `submission/学号-姓名/` 根目录。
2. 确认从本地 Git 邮箱推断出的学号 `241098117` 和姓名 `张城宗` 正确。
3. 运行以下命令生成最终压缩包：

```powershell
powershell -ExecutionPolicy Bypass -File ".\submission\生成最终压缩包.ps1" -StudentId "241098117" -Name "张城宗"
```

不要把 `提交前说明.md` 放进最终压缩包。
